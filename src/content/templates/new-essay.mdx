<%*
const title = await tp.system.prompt("Essay title");
const slug = title
  .toLowerCase()
  .normalize("NFKD")
  .replace(/[^a-z0-9]+/g, "-")
  .replace(/^-+|-+$/g, "");
await tp.file.move(`drafts/${slug}/${slug}`);
const publicationDay = tp.date.now("YYYY-MM-DD");
-%>
---
title: <% title %>
date: <% publicationDay %>
updated:
sticky: false
cornerstone: false
excerpt:
newsletterIntro: |-

categories:
tags:
cover:
coverAlt:
coverCaption:
downloads:
audio:
---

import QuickSummary from "../../../components/content/QuickSummary.astro";

<QuickSummary>

-

</QuickSummary>
