---
import Divider from "./Divider.astro";
import Image from "./Image.astro";
import ReadTime from "./ReadTime.astro";

import { type Post } from "../types.ts";

interface Props {
  post: Post;
}

const { post } = Astro.props;
const postDate = new Date(post.date).toLocaleString("default", {
  day: "numeric",
  month: "long",
  year: "numeric",
});
---

<section
  id="highlight"
  class="border-line mx-auto my-10 grid max-w-6xl grid-cols-1 items-center gap-16 border-y px-10 pt-8 pb-16 min-[820px]:grid-cols-[1.35fr_1fr]"
>
  <Image
    src={post.featuredImage!}
    alt={post.featuredImageAlt ?? ""}
    width={1600}
    height={900}
    widths={[580, 1160]}
    sizes="(max-width: 820px) 100vw, 580px"
    class="w-full"
    priority
  />
  <div class="flex-col space-y-6">
    <div class="flex items-center gap-3">
      <span class="label whitespace-nowrap">
        {post.categories.join(", ")}
      </span>
      <Divider width={20} />
      <span class="meta whitespace-nowrap">Lead essay</span>
    </div>
    <h2><a href={post.url}>{post.title}</a></h2>
    <p class="lead">{post.excerpt}</p>
    <p class="meta">
      {postDate} &middot; <ReadTime content={post.contentHtml} />
    </p>
  </div>
</section>
