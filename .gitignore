dist/
.astro/
node_modules/
local/
public/_redirects
.obsidian/

*.log*
.env
.env.production

# Wrangler config (keeps R2 bucket names out of the repo) + local state
wrangler.toml
.wrangler/

# Generated essay audio narrations (npm run audio) — kept locally next to each
# essay and uploaded to R2, but never committed (binary bloat; R2 is the source)
src/content/**/*.mp3
# Editable narration scripts (npm run audio) — regenerated on demand.
src/content/**/*.audio.txt

# Local agent tooling config (launch.json, per-machine permissions). The shared
# context in .agents/ IS committed.
.claude/

.DS_Store

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
