# /_astro/ is content-hashed, so a change ships a new URL and these can be
# immutable. HTML stays on the default so deploys show up immediately.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
