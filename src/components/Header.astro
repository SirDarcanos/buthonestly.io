---
import Divider from "./Divider.astro";
import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

import { getCategories } from "../lib/essays.ts";

const categories = await getCategories();
const categoryLinks = categories.map((category) => ({
  href: `/section/${category.slug}/`,
  label: category.name,
}));
---

<header id="header" class="mx-auto w-full max-w-6xl px-6 focus:outline-none">
  <div class="border-ink flex items-center gap-8 border-b py-5">
    <Logo priority />
    <span class="elegant hidden md:inline-block"
      >Clarity without the comfort.</span
    >
    <Divider />
    <Nav
      class="font-medium"
      linkClass="text-muted"
      links={[
        {
          href: "/",
          label: "Home",
        },
        ...categoryLinks.map((link) => ({
          href: link.href,
          label: link.label,
        })),
      ]}
    />
    <a
      href="/search/"
      aria-label="Search"
      class="text-muted hover:text-accent flex-none transition-colors"
    >
      <svg
        class="size-5"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        aria-hidden="true"
      >
        <circle cx="11" cy="11" r="7"></circle>
        <path d="m21 21-4.3-4.3"></path>
      </svg>
    </a>
  </div>
</header>
