dist/
.astro/
node_modules/
local/
data/link-graph.json
docs/research/
artifacts/lighthouse/
artifacts/lighthouse-smoke/
artifacts/publication-monitoring-handoff.json

*.log*
.env
.env.production

# Wrangler config (keeps R2 bucket names out of the repo) + local state
wrangler.toml
.wrangler/

# Narration artifacts belong to the separate manual tool; R2 owns published audio.
src/content/**/*.mp3
src/content/**/*.audio.txt

# Local agent tooling config, per-machine permissions, and private context.
.agents/product-marketing.md
.claude/
.pi/

.DS_Store

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
